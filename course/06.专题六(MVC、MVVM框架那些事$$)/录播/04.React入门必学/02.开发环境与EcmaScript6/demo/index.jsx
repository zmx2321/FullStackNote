const React = require("react");

let index = React.createClass({
    render: function() {
        return (
            <div className="test">第一个React</div>
        );
    }
});

module.export = index;